version: 0.0
os: linux

files:
  - source: scripts
    destination: /home/ubuntu/backend-arqui/
permissions:
  - object: scripts
    pattern: "**"
    owner: ubuntu
hooks:
  ApplicationStop:
    - location: app/stop.sh
      timeout: 600
      runas: ubuntu
  AfterInstall:
    - location: app/install.sh
      timeout: 600
      runas: ubuntu
  ApplicationStart:
    - location: app/start.sh
      timeout: 600
      runas: ubuntu
